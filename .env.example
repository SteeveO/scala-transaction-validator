# Copy to .env and fill in a real secret before running docker-compose up.
# Generate one with: openssl rand -base64 32
APPLICATION_SECRET=changeme

# Host port the app is exposed on (container always listens on 9000).
PORT=9000
